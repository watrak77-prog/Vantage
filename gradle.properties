org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

# Loom does not support the configuration cache.
org.gradle.configuration-cache=false

# Mod
mod_version = 0.1.0
maven_group = pl.watrak
archives_base_name = vantage

# Java toolchains are deliberately not pinned here: the paths would be whatever
# they happen to be on one machine. Point Gradle at your own JDKs from
# ~/.gradle/gradle.properties instead — see the README for what is needed.
